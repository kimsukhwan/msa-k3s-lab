server:
  port: 8093

spring:
  application:
    name: gateway

management:
  endpoints:
    web:
      exposure:
        include: prometheus,health

backend:
  order-service-url: ${ORDER_SERVICE_URL:http://localhost:8092}
  product-service-url: ${PRODUCT_SERVICE_URL:http://localhost:8091}
