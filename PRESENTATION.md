# 뱅킹시스템 아키텍처 — 전체 흐름 발표 대본

근거: `infra/architecture.drawio` · 다이어그램을 위→아래로 따라가는 구성 · 총 20분 기준(질의응답 별도)

> **발표 팁** — drawio는 세로로 긴 문서다. 발표 전에 ①최상단(클라이언트~AWS), ②L4~클러스터
> 상단, ③Layer B/A, ④우측 VM 컬럼, ⑤하단 체크리스트·비교 섹션, 다섯 화면 위치를 미리
> 잡아두고 스크롤 동선을 연습해 두면 흐름이 끊기지 않는다.

## 진행 순서

| # | 구간 | 시간 |
|---|------|------|
| 01 | 한 장 요약 — 두 세계와 그 사이 | 2분 |
| 02 | 진입 계층 — 직원·고객·채널계 | 3분 |
| 03 | L4 로드밸런서 — 모든 트래픽의 첫 관문 | 2분 |
| 04 | Nutanix HCI와 k8s 클러스터 | 2분 |
| 05 | 클러스터 내부 — 세 개의 레이어 | 4분 |
| 06 | 데이터·미들웨어 — k8s 안과 밖 | 3분 |
| 07 | 플랫폼 구성요소와 배포 | 2분 |
| 08 | 인증 흐름 — 직원 vs 고객 | 2분 |
| 09 | 남은 결정 사항 (체크리스트) — 마무리 | 2분 |

---

## 01. 한 장 요약 — 두 세계와 그 사이 (2분)

**가리킬 곳**: 다이어그램 최상단 전체 — 왼쪽 클라이언트·슈퍼앱 박스와 오른쪽 AWS 박스,
그 아래 큰 Nutanix 박스가 한눈에 들어오게 축소한 상태

> 이 시스템은 크게 두 세계로 나뉩니다. 오른쪽 위 주황색 영역이 **AWS 채널계**인데,
> 슈퍼앱과 채널BFF가 있는 곳이고 우리 운영 범위 밖입니다. 그리고 아래 큰 회색 박스가
> 우리가 운영하는 **내부망 IDC** — Nutanix 가상화 인프라 위에 k8s 클러스터와 주변
> VM들이 올라가 있습니다.
>
> 두 세계는 DX, 즉 전용회선으로 연결됩니다. 중요한 건 DX가 사설 회선일 뿐 암호화나
> 호출자 신원을 보장하지는 않는다는 점입니다. 그래서 그 위에 편도 TLS, 매 요청 JWT 검증,
> 그리고 mTLS 채널 인증을 겹겹이 얹는 구조로 설계했습니다. 오늘은 요청이 들어오는 길을
> 따라 위에서 아래로 훑겠습니다.

- 청중이 기억해야 할 문장 하나: "채널계는 AWS, 계정계는 내부망 — 그 사이는 DX + 검증 계층"
- 여기서는 박스 이름을 다 읽지 말 것 — 두 영역의 구분만 각인시키고 다음으로

## 02. 진입 계층 — 직원·고객·채널계 (3분)

**가리킬 곳**: 최상단 세 박스 — 클라이언트(내부망 직원전용) → 슈퍼앱(고객전용) → AWS(채널계) 순서로

> 들어오는 사용자는 두 부류입니다. 첫째, **내부망 직원** — 왼쪽 노란 박스입니다. 사내에서
> React 웹으로 접속하고, 우리 auth-service에 로그인해서 토큰을 받습니다.
>
> 둘째, **슈퍼앱 고객**입니다. 여기가 특이한데, 고객은 우리 시스템에 로그인하지 않습니다.
> 슈퍼앱에 이미 로그인된 상태로 들어오고, 슈퍼앱이 서명한 토큰과 고객 키값을 들고 옵니다.
> 그 토큰을 AWS의 **채널BFF**가 1차로 검증해서 고객 키값을 추출하고, 화면에 맞는 응답을
> 조합해 DX로 내려보냅니다.
>
> AWS 영역에는 두 가지가 더 있습니다. **슈퍼앱 JWKS 엔드포인트** — 슈퍼앱이 공개하는
> 서명 검증용 공개키 목록인데, 발급 주체가 슈퍼앱이라는 게 핵심입니다. 우리는 검증만 하고
> 발급은 못 합니다. 그리고 **외부 Proxy** — 이건 나가는 문인데, 우리 openapi 서비스가
> 마이데이터·스크래핑 등으로 나갈 때 씁니다. 뒤에서 다시 나옵니다.

- "고객은 우리한테 로그인하지 않는다"가 이 구간의 핵심 — 인증 책임의 경계가 여기서 갈림
- JWKS는 "공개키 목록" 한 마디로 — 상세 원리는 08에서

**Q. 채널BFF가 이미 검증했는데 우리도 또 검증하나?**
A. 한다. 채널BFF는 우리 운영 범위 밖(AWS)이라 "남의 검증"이다. 내부망 진입점인 gateway가
같은 토큰을 2차로 다시 검증한다 — 신뢰 경계가 다르기 때문. JWKS 공개키 방식이라 검증
지점이 늘어도 비밀 공유가 없어 부담이 없다.

## 03. L4 로드밸런서 — 모든 트래픽의 첫 관문 (2분)

**가리킬 곳**: Nutanix 박스 상단의 노란 가로 박스 — "L4 로드밸런서 (NetScaler MPX 9230)"

> 내부망으로 들어오는 모든 트래픽은 이 L4 장비를 먼저 지납니다. 실제 장비는 **Citrix
> NetScaler MPX 9230**입니다. 하는 일은 두 가지입니다.
>
> 첫째, **SSL 종료**입니다. HTTPS 암호를 여기서 풀고, 그 뒤로는 평문 HTTP로 전달합니다.
> 인증서 관리를 장비 한 곳으로 모으기 위한 선택이고, 대신 원 요청 정보를 X-Forwarded
> 헤더로 실어 보냅니다.
>
> 둘째, **슈퍼앱 전용 VIP에서는 mTLS 종료까지** 검토하고 있습니다. 슈퍼앱 트래픽에만
> 클라이언트 인증서를 요구해서, "이 회선으로 들어온 호출자가 진짜 채널BFF인가"를 TLS
> 수준에서 증명하는 겁니다. 이 기능이 지금 장비·라이선스로 가능한지가 관건이었는데,
> **확인이 끝났습니다** — 클라이언트 인증서 검증, 헤더 삽입, 헤더 제거 세 기능 모두 현재
> 라이선스에 포함돼 있어 별도 구매 없이 구성할 수 있습니다.

- "확정"이 아니라 "장비 지원 확인 완료" — 표현 주의 (최종 채택 결정은 별도)
- mTLS 상세 구성 순서는 다이어그램 하단에 별도 섹션 있음 — 질문 나오면 그리로 스크롤

**Q. 클러스터 안이 평문이면 보안상 문제 없나?**
A. L4 이후 구간은 폐쇄망 전제다. 여기에 더해 Calico NetworkPolicy로 "gateway 인바운드는
Envoy Gateway만" 같은 통신 제한을 강제하는 것을 체크리스트 ⑤에 올려뒀다. 내부 구간
암호화 요구가 생기면 cert-manager 도입과 함께 재검토한다.

## 04. Nutanix HCI와 k8s 클러스터 (2분)

**가리킬 곳**: 큰 회색 컨테이너 테두리 → 그 안의 k8s 클러스터 박스 제목줄(kubeadm · Master 3 + Worker 3)

> 이 큰 박스 전체가 **Nutanix HCI**, 우리 IDC의 가상화 인프라입니다. 그 위에 VM으로 두
> 부류가 올라갑니다 — 왼쪽의 **k8s 클러스터**와, 오른쪽 컬럼의 **클러스터 밖 VM들**(DB,
> 스토리지, 프록시, 전자문서 서버). 왜 나눴는지는 06에서 설명하겠습니다.
>
> 클러스터는 kubeadm으로 직접 구축합니다. Master 3대, Worker 3대 — Master가 3대인 이유는
> 클러스터의 두뇌인 etcd가 다수결(쿼럼)로 동작해서, 1대가 죽어도 나머지 2대로 버티기
> 위해서입니다. Master 앞에는 API 서버 VIP를 둬서 어느 Master가 죽어도 접속 지점이
> 유지됩니다.

- 관리형(EKS 등)이 아니라 직접 구축 — 그래서 CNI·Gateway 구현체·metrics-server 등을 전부 골라 설치 (07 복선)
- VM 배치 시 Master 3대를 서로 다른 물리 노드에 분산 — 체크리스트 ①

## 05. 클러스터 내부 — 세 개의 레이어 (4분)

**가리킬 곳**: Envoy Gateway 박스 → 파란 Layer C → 초록 Layer B → 빨간 Layer A 순서로 위에서 아래로

> 클러스터 안 첫 관문은 **Envoy Gateway**입니다. L4가 넘겨준 평문 트래픽을 NodePort로
> 받아서, URL 경로를 보고 어느 서비스로 보낼지 라우팅만 합니다. 참고로 이건 우리가 만든
> 앱이 아니라 직접 설치한 오픈소스 인프라입니다.
>
> 그 아래부터 우리 서비스가 세 레이어로 나뉩니다. 파란색 **Layer C**는 화면 중심 —
> Next.js 기반 web BFF가 화면 단위로 응답을 조합하고, **gateway**가 모든 요청의 JWT를
> 2차 검증합니다. 직원 토큰과 고객 토큰을 모두 여기서 처리합니다.
>
> 초록색 **Layer B**가 업무의 본체입니다. 여신(loan), 채권(bond), 수신(deposit),
> 고객(customer), 상담(callcenter), 직원·조직(user), 공통·권한(system), 메시징(ums) —
> 여덟 개 도메인 서비스이고, 각자 자기 DB를 갖습니다. 서로 필요한 데이터는 DB를 조인하는
> 게 아니라 REST 호출로 받아옵니다.
>
> 빨간색 **Layer A**는 외부 계정계 통신 전담입니다. **fep**는 전문(電文) 통신,
> **openapi**는 HTTP/JSON 통신을 맡습니다. 그리고 중요한 규칙 하나 — **호출은 위에서
> 아래로만 흐릅니다.** C가 B를 부르고 B가 A를 부르지, A가 B를 거꾸로 부르는 일은
> 없습니다. 역방향을 허용하면 순환 의존이 생겨서 배포 순서가 꼬이고 장애가 번집니다.

- Envoy Gateway ≠ gateway 서비스 — 이름이 겹치니 "설치한 인프라" vs "우리가 만든 앱"으로 구분해 말하기
- Layer B 안에서도 system은 최하위 — 아무도 호출하지 않는 공통 참조 계층
- 인증 경계는 gateway 하나 — 통과한 요청만 하위로, 하위 서비스는 재검증 안 함(내부 신뢰)

**Q. 서비스가 여덟 개면 너무 잘게 쪼갠 것 아닌가?**
A. 기준은 코드 크기가 아니라 데이터 소유다. 서비스마다 자기 DB를 갖고, 어떤 테이블을
누가 소유하는지가 분리 기준이라 도메인 수만큼 나뉜 것. 반대로 화면 로직은 web BFF 한 곳에
모아 잘게 쪼개지 않았다.

## 06. 데이터·미들웨어 — k8s 안과 밖 (3분)

**가리킬 곳**: 클러스터 하단의 Redis·Kafka·LGTM 박스 → 오른쪽 컬럼의 PostgreSQL·내부 Proxy In/Out·NFS·EDMS

> 데이터 계층은 "클러스터 안에 둘 것"과 "밖에 둘 것"을 성격에 따라 나눴습니다.
>
> **클러스터 안**에는 Redis와 Kafka가 있습니다. Redis는 Sentinel 3노드로 캐시와 세션을,
> Kafka는 KRaft 3노드로 이벤트를 맡습니다. 둘 다 마스터 1대가 죽어도 자동 승격으로 버티는
> HA 구성입니다. 관측 스택 LGTM — 로그·시각화·추적·지표 — 도 클러스터 안에 있습니다.
>
> **클러스터 밖** 오른쪽 컬럼을 보시면, 먼저 **PostgreSQL**이 VM 3대의 Patroni HA로
> 돕니다. DB를 컨테이너로 넣지 않은 건 데이터의 수명이 클러스터의 수명과 달라야 하기
> 때문입니다. 서비스들은 ExternalName이라는 내부 별칭으로 접속해서, DB 위치가 바뀌어도
> 코드는 안 바뀝니다.
>
> 그 아래 **내부 Proxy가 Out과 In 두 개**입니다. Out은 fep가 중앙회·신용정보원으로 나가는
> 문, In은 오토비긴즈·NICE DNR·카히스토리·쿠콘 같은 외부 기관이 들어오는 문입니다. 화살표
> 방향이 반대인 걸 봐주세요 — 나가는 문과 들어오는 문을 분리해 관리합니다.
>
> 마지막으로 **전자문서 스택** — EDMS API, Clip eForm, StreamDocVu 세 가지 상용 솔루션은
> 라이선스 정책상 컨테이너로 올리지 않고 전용 VM으로 운영합니다. 이중화용 VM 1대를 추가할
> 예정이고, Layer B 서비스들이 호출해서 씁니다.

- 안/밖 구분 기준 한 문장: "상태(데이터)의 수명이 클러스터와 달라야 하면 밖으로"
- NFS는 파일용(5TB)·로그용(10TB) 분리 — LGTM 로그의 최종 저장소는 로그용 NFS
- EDMS 이중화 VM 2대는 서로 다른 물리 노드에 분산 (체크리스트 ①)

**Q. Kafka도 상태가 있는데 왜 클러스터 안인가?**
A. Kafka는 3노드 복제로 스스로 데이터를 지키는 구조라 파드 재배치에 견딘다. 단 저장소
궁합 문제로 PVC는 NFS 대신 로컬 디스크를 권장한다 — 다이어그램에도 명시돼 있다.

## 07. 플랫폼 구성요소와 배포 (2분)

**가리킬 곳**: 클러스터 하단의 보라색 박스들 — CoreDNS · Calico · ArgoCD · metrics-server · NFS 프로비저너

> 보라색 박스들은 서비스가 아니라 클러스터를 떠받치는 부품들입니다. 관리형 클라우드라면
> 알아서 딸려오는 것들인데, 직접 구축이라 하나하나 골라 설치합니다. **Calico**는 파드
> 네트워크 자체를 만들고, **CoreDNS**는 서비스 이름을 IP로 바꿔주고, **metrics-server**는
> 자원 사용량 측정과 자동 스케일링의 기반이 됩니다.
>
> 배포는 **ArgoCD의 GitOps** 방식입니다. 개발자가 git에 push하면 CI가 이미지를 만들고,
> ArgoCD가 git의 선언 상태를 감지해 클러스터에 자동 반영합니다. **사람이 직접 kubectl로
> 배포하지 않는다**는 게 원칙이고, 예외는 Secret 하나입니다 — 비밀번호와 키는 git에 두지
> 않으므로 별도로 직접 주입합니다.

- cert-manager는 의도적으로 미설치 — TLS 종료가 전부 L4라 클러스터 안에 관리할 인증서가 없음
- "git에 없는 건 Secret뿐" — 보안 질문이 나오면 이 문장으로 정리

## 08. 인증 흐름 — 직원 vs 고객 (2분)

**가리킬 곳**: 다이어그램 하단 "인증 경로 비교" 섹션 — 노란 컬럼(직원)과 주황 컬럼(고객) 나란히

> 하단에 두 인증 경로를 나란히 정리해 뒀습니다. 차이는 세 가지입니다.
>
> 첫째, **토큰 발급자**. 직원 토큰은 우리 auth-service가 발급하고, 고객 토큰은 슈퍼앱이
> 발급합니다. 둘째, **검증 방식**. 직원은 대칭키 공유 방식이고, 고객은 슈퍼앱의
> 공개키(JWKS)로 검증만 합니다 — 비밀을 공유하지 않아서 검증하는 쪽이 늘어나도 키 배포
> 문제가 없습니다. 셋째, **채널 보호**. 직원은 내부망 안이지만, 고객 트래픽은 외부에서
> DX를 타고 오므로 L4의 mTLS 채널 인증을 추가로 얹습니다.
>
> 그리고 두 경로 모두 **gateway 한 곳으로 합류**합니다. gateway가 토큰의 발급자 정보를
> 보고 직원용·고객용 검증을 분기하고, 통과한 요청만 하위로 내려보냅니다. 인증 경계가 한
> 곳이라 하위 서비스는 인증 로직을 전혀 모릅니다.

- 바로 아래 "요청 검증 시퀀스"와 "mTLS 처리 순서" 섹션이 이어짐 — 시간 남으면 mTLS 4단계(핸드셰이크→헤더 제거→삽입→전달)까지, 없으면 생략 가능
- mTLS ②→③ 순서(제거가 삽입보다 먼저)가 위조 헤더 차단의 핵심 — 기술 청중이면 짚어줄 것

## 09. 남은 결정 사항 — 체크리스트로 마무리 (2분)

**가리킬 곳**: 하단 체크리스트 ①~⑦ — 점선 박스 두 줄

> 마지막으로, 이 다이어그램은 확정안이 아니라 **결정해야 할 것들을 함께 담은 문서**입니다.
> 하단 체크리스트에 정리돼 있습니다.
>
> ①~③은 인프라 운영 — VM 물리 분산, etcd 백업, 인증서 만료 관리, NFS 단일 장애점과 DB
> 백업 전략입니다. ④가 슈퍼앱 mTLS인데, 장비 지원 확인은 끝났고 **custKey를 내부
> 고객번호로 바꾸는 지점**과 **고객 토큰의 로그아웃 즉시 폐기** 두 가지가 미해결로 남아
> 있습니다. ⑤~⑦은 구축 시 챙길 것들 — 네트워크 대역이 사내망·AWS와 겹치지 않는지, Secret
> 암호화, 시간 동기화 같은 항목입니다.
>
> 정리하면 — 오늘 보신 구조의 뼈대는 서 있고, 남은 것은 이 체크리스트를 하나씩 확정해
> 나가는 일입니다. 질문 받겠습니다.

| 구분 | 상태 | 핵심 항목 |
|------|------|-----------|
| ①~③ 인프라 운영 | 확정 필요 | VM 물리 분산 · etcd 백업 · kubeadm 인증서 만료 · NFS SPOF · DB WAL 백업 |
| ④ 슈퍼앱 mTLS | 장비 지원 확인 완료 · 2건 미해결 | custKey→cust_unno 변환 지점 · jti(로그아웃 즉시 폐기) |
| ⑤~⑦ 구축 체크 | 구축 시 확인 | CIDR 충돌 · NetworkPolicy · Secret 암호화 · RBAC · 시간 동기화 · 레지스트리 CA |

---

## 부록 — 예상 질문 모음

**Q. 왜 관리형 쿠버네티스(EKS 등)를 안 쓰나?**
A. 계정계는 내부망 IDC에 있어야 한다는 전제(금융 인프라·전용선 연계) 때문에 온프레미스
직접 구축이다. 그 대가로 CNI·Gateway·metrics-server 등을 직접 설치·운영하며, 그 목록과
이유가 다이어그램에 전부 명시돼 있다.

**Q. mTLS는 언제 확정되나?**
A. 기술 검증(장비 지원)은 끝났다. 남은 것은 최종 채택 결정과, 하기로 하면 슈퍼앱 측과의
인증서 수명주기 합의(CA 배포·만료 모니터링·교체 절차)다. 종료 지점 3옵션 비교 검토안이
별도 문서("mTLS 검토안" 탭)로 있다.

**Q. 장애가 나면 어디를 보나?**
A. LGTM 스택이 로그(Loki)·추적(Tempo)·지표(Mimir)를 통합 수집하고 Grafana로 본다.
요청마다 추적 ID가 헤더로 전 서비스에 전파되므로 한 요청의 이동 경로를 서비스 경계를
넘어 묶어 볼 수 있다.

**Q. 고객 개인정보는 어떻게 보호되나?**
A. 고객 식별은 슈퍼앱의 고객 키값으로 오가고, 내부 고유번호와의 변환은 내부에서만
한다(변환 지점 확정은 체크리스트 ④). API 응답의 개인정보 마스킹과 로그 마스킹은 전
서비스 공통 모듈로 강제한다.

**Q. EDMS 서버가 죽으면 업무가 멈추나?**
A. 그래서 이중화 VM 1대를 추가할 예정이고, 두 VM을 서로 다른 물리 노드에 분산한다.
Active/Standby 전환 방식과 Standby 라이선스 필요 여부는 확정 대상으로 남겨뒀다.
